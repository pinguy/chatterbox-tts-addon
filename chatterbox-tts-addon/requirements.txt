# The Firefox extension runtime has no separate Python dependencies.
# Run install.sh from the complete repository to create the backend virtual
# environment and install the Chatterbox/Flask/audio dependencies it needs.
